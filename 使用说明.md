# PDS 云盘工具使用说明

本工具用于查看阿里云 PDS 空间、生成文件清单，以及将云盘文件下载到本地。程序只读取云端数据，不会上传、修改或删除云盘文件。

## 1. 新环境准备

目标环境需要满足：

- Linux（x86_64 或 ARM64）
- Python 3.11 或更高版本
- 能够访问阿里云 PDS 服务

从私有 GitHub 仓库克隆项目：

```bash
git clone https://github.com/zzi4/pds-drive-sync.git
cd pds-drive-sync
chmod +x pds-sync
```

当前电脑中的项目绝对路径为：

```text
/home/stu1/Downloads/pds-drive-sync
```

工具不需要安装 Python 第三方依赖。首次执行初始化时，如果系统没有 Aliyun CLI，程序会自动在项目的 `.tools/` 目录中安装一份私有版本，不需要 `sudo`。

## 2. 配置 API Key

使用前需要准备：

- PDS API Key
- 所属 PDS 域 ID
- PDS 服务地址，通常为 `https://<域ID>.api.aliyunfile.com`

本项目当前默认域 ID 为 `bj39311`，对应地址为：

```text
https://bj39311.api.aliyunfile.com
```

如果使用默认域，执行：

```bash
./pds-sync setup
```

如果使用其他域，执行：

```bash
./pds-sync setup \
  --domain-id <域ID> \
  --pds-endpoint https://<域ID>.api.aliyunfile.com
```

命令运行后会提示输入 API Key：

```text
PDS API Key（输入不会显示，将保存到私密文件）:
```

直接粘贴 API Key 并按回车即可。输入内容不会显示在终端中。

API Key 会保存到当前 Linux 用户的以下文件中：

```text
~/.config/pds-sync/api_key
```

当前电脑中的绝对保存位置为：

```text
/home/stu1/.config/pds-sync/api_key
```

该文件权限固定为 `600`。不要把 API Key 写入 `config.toml`，也不要将该凭据文件提交到 Git。每个新环境、每个 Linux 用户都需要单独执行一次 `setup`。

## 3. 使用方法

以下命令都需要在项目目录中执行。

### 查看账号和云盘空间

```bash
./pds-sync status
```

该命令用于验证认证是否成功，并显示当前账号可访问的个人、团队和企业空间。

### 获取文件清单

```bash
./pds-sync inventory
```

该命令只获取云盘文件与文件夹信息，不下载文件。结果保存在：

```text
snapshots/<UTC时间>/
```

### 下载全部文件

```bash
./pds-sync fetch
```

`fetch` 会先重新获取文件清单，然后下载当前可访问空间中的全部文件，因此不需要提前执行 `inventory`。

下载文件保存在：

```text
downloads/<空间类型>-<drive_id>/<云端路径>
```

如果本地文件与云端哈希一致，程序会跳过下载；如果无法确认一致，则保留原文件，并将新文件保存为冲突副本，不会直接覆盖原文件。

## 4. 新环境完整操作示例

使用默认域时，依次执行：

```bash
git clone https://github.com/zzi4/pds-drive-sync.git
cd pds-drive-sync
chmod +x pds-sync
./pds-sync setup
./pds-sync status
./pds-sync fetch
```

完成首次 `setup` 后，日常使用通常只需要：

```bash
./pds-sync status
./pds-sync fetch
```
